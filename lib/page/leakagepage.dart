import 'package:flutter/cupertino.dart';

class LeakagePage extends StatelessWidget {
  const LeakagePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
